---
phase: 09-xai-audit-ledger
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - backend/run_simulation.py
  - backend/routes.py
  - outputs/.gitkeep
autonomous: true
requirements:
  - REQ-028
must_haves:
  truths:
    - "A daily JSON ledger file can be read by non-engineers."
    - "Frontend/API can fetch latest XAI ledger."
  artifacts:
    - path: "outputs/audit_ledger_YYYY-MM-DD_dayXXX.json"
      provides: "Daily regulator-ready ledger"
    - path: "backend/routes.py"
      provides: "Ledger retrieval endpoint"
  key_links:
    - from: "run_simulation.py"
      to: "outputs/"
      via: "daily ledger export call"
    - from: "routes.py"
      to: "outputs/"
      via: "/api/xai-audit-ledger"
---

<objective>
Compile and export the 7-phase ledger as a stable daily artifact and expose retrieval API.

Purpose: make XAI explainability consumable for regulators/operators.
Output: JSON ledger files + API endpoint.
</objective>

<context>
@backend/run_simulation.py
@backend/routes.py
@backend/src/agents/xai/audit_ledger.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Wire ledger export into daily simulation loop</name>
  <files>backend/run_simulation.py</files>
  <action>At end-of-day flow, finalize daily ledger, write to outputs with stable file naming, and include export path in simulation summary payload.</action>
  <verify>
    <automated>python backend/main.py</automated>
  </verify>
  <done>Simulation run writes at least one `audit_ledger_*.json` file with 7-phase trace.</done>
</task>

<task type="auto">
  <name>Task 2: Add API for latest/day-specific ledger retrieval</name>
  <files>backend/routes.py</files>
  <action>Add endpoint(s) to fetch latest ledger and optional date/day query. Return explicit 404 with actionable message when absent.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_routes_xai_ledger.py -q</automated>
  </verify>
  <done>API returns ledger JSON and supports frontend consumption.</done>
</task>
</tasks>

<verification>
Validate schema presence for `phase_trace`, `executive_summary`, and compliance fields.
</verification>

<success_criteria>
Operators can inspect a full-day human-readable decision chain through file or API.
</success_criteria>

<output>
After completion, create `.planning/phases/09-xai-audit-ledger/09-02-SUMMARY.md`
</output>

