---
phase: 10-delta-orchestration
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - backend/run_simulation.py
  - backend/routes.py
  - outputs/api_cost_savings.csv
autonomous: true
requirements:
  - REQ-031
must_haves:
  truths:
    - "Daily run reports LLM wake/sleep and savings."
    - "Cost-savings artifact is generated and queryable."
  artifacts:
    - path: "outputs/api_cost_savings.csv"
      provides: "Per-day cost savings accounting"
  key_links:
    - from: "engine summaries"
      to: "api_cost_savings.csv"
      via: "daily append/export"
---

<objective>
Add measurable cost accounting and expose orchestration performance to API consumers.

Purpose: prove two-tier architecture business value.
Output: savings CSV and API surface for dashboarding.
</objective>

<context>
@backend/src/orchestration/engine.py
@backend/run_simulation.py
@backend/routes.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Persist per-day API cost metrics</name>
  <files>backend/run_simulation.py, outputs/api_cost_savings.csv</files>
  <action>Append per-day records for baseline-only vs full-LLM cost, wake decision, and realized savings percentage.</action>
  <verify>
    <automated>python backend/main.py</automated>
  </verify>
  <done>CSV contains one row per day with non-null savings fields.</done>
</task>

<task type="auto">
  <name>Task 2: Expose savings data via API</name>
  <files>backend/routes.py</files>
  <action>Add endpoint to return summary stats and row-level savings data for frontend charts.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_routes_cost_savings.py -q</automated>
  </verify>
  <done>API endpoint returns latest cost-savings summary and historical rows.</done>
</task>
</tasks>

<verification>
Validate savings are >0 on runs that include baseline-only days.
</verification>

<success_criteria>
Project can demonstrate concrete compute-cost reduction from architecture design.
</success_criteria>

<output>
After completion, create `.planning/phases/10-delta-orchestration/10-02-SUMMARY.md`
</output>

