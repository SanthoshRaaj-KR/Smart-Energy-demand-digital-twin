---
phase: 10-delta-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/orchestration/engine.py
  - backend/main.py
autonomous: true
requirements:
  - REQ-029
  - REQ-030
must_haves:
  truths:
    - "Stage 1-4 execution order is always preserved."
    - "LLM-heavy routing runs only when Delta > threshold."
  artifacts:
    - path: "backend/src/orchestration/engine.py"
      provides: "Single source orchestration flow"
  key_links:
    - from: "main.py"
      to: "engine.py"
      via: "orchestrator bootstrap"
---

<objective>
Create a single orchestration engine that enforces the entire pipeline lifecycle day-by-day.

Purpose: remove scattered flow control and guarantee stage ordering.
Output: reusable `OrchestrationEngine` with clear contracts.
</objective>

<context>
@backend/main.py
@backend/run_simulation.py
@backend/src/agents/intelligence_agent/orchestrator.py
@backend/src/agents/routing_agent/unified_routing_orchestrator.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement engine contract and day loop</name>
  <files>backend/src/orchestration/engine.py</files>
  <action>Implement orchestration class that runs Stage 1 once, Stage 2 daily, Stage 3 conditionally on Delta, and Stage 4 at end of day. Return structured per-day summary.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_orchestration_engine.py -q</automated>
  </verify>
  <done>Engine returns deterministic stage outcomes for normal/anomaly days.</done>
</task>

<task type="auto">
  <name>Task 2: Wire main entrypoint to engine</name>
  <files>backend/main.py</files>
  <action>Refactor main execution path to delegate stage control to engine while preserving existing CLI outputs and artifacts.</action>
  <verify>
    <automated>python backend/main.py</automated>
  </verify>
  <done>Main run uses new engine and still completes without stage-skipping regressions.</done>
</task>
</tasks>

<verification>
Confirm Stage 3 is skipped when Delta is zero and executed when Delta is positive.
</verification>

<success_criteria>
Pipeline control is centralized with no duplicate orchestration logic.
</success_criteria>

<output>
After completion, create `.planning/phases/10-delta-orchestration/10-01-SUMMARY.md`
</output>

