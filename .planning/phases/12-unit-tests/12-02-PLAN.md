---
phase: 12-unit-tests
plan: 02
type: tdd
wave: 2
depends_on:
  - 12-01
files_modified:
  - backend/tests/test_lifeboat_protocol.py
  - backend/tests/test_llm_parameter_autopsy.py
  - backend/tests/test_memory_edge_cases.py
autonomous: true
requirements:
  - REQ-036
must_haves:
  truths:
    - "Emergency islanding and autopsy learning behaviors are validated."
    - "Memory buffer constraints are regression-protected."
  artifacts:
    - path: "backend/tests/test_lifeboat_protocol.py"
      provides: "Graph-cut safety logic checks"
    - path: "backend/tests/test_llm_parameter_autopsy.py"
      provides: "Autopsy patch generation checks"
  key_links:
    - from: "lifeboat/autopsy tests"
      to: "agent implementations"
      via: "deterministic fixtures"
---

<objective>
Complete unit-test expansion for patent-critical flows and memory edge cases.

Purpose: prevent silent regressions in safety and self-healing features.
Output: focused tests for lifeboat, autopsy, and memory corner cases.
</objective>

<context>
@backend/src/agents/routing_agent/lifeboat_protocol.py
@backend/src/agents/fusion_agent/llm_parameter_autopsy.py
@backend/src/agents/routing_agent/unified_routing_orchestrator.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add lifeboat protocol safety tests</name>
  <files>backend/tests/test_lifeboat_protocol.py</files>
  <behavior>
    - Frequency emergency triggers islanding decision.
    - Selected cut preserves maximum surviving capacity.
  </behavior>
  <action>Author deterministic topology fixtures and validate trigger threshold, cut-set choice, and post-cut safety metrics.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_lifeboat_protocol.py -q</automated>
  </verify>
  <done>Lifeboat decisions are reproducible and constrained by policy thresholds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add autopsy + memory edge-case tests</name>
  <files>backend/tests/test_llm_parameter_autopsy.py, backend/tests/test_memory_edge_cases.py</files>
  <behavior>
    - Monthly autopsy emits valid parameter patch schema.
    - Memory buffer enforces 3-day cap with correct eviction order.
  </behavior>
  <action>Test patch generation from synthetic failures and enforce buffer/eviction behavior under repeated failures.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_llm_parameter_autopsy.py backend/tests/test_memory_edge_cases.py -q</automated>
  </verify>
  <done>Self-healing and memory controls are regression-safe.</done>
</task>
</tasks>

<verification>
Run all unit tests together after additions and ensure stable pass.
</verification>

<success_criteria>
Patent-grade behaviors are backed by deterministic tests.
</success_criteria>

<output>
After completion, create `.planning/phases/12-unit-tests/12-02-SUMMARY.md`
</output>

