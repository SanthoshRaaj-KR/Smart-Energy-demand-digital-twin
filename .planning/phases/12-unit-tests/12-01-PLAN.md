---
phase: 12-unit-tests
plan: 01
type: tdd
wave: 1
depends_on:
  - 09-02
  - 10-02
files_modified:
  - backend/tests/test_forward_market_planner.py
  - backend/tests/test_intelligence_orchestrator.py
  - backend/tests/test_dr_bounty_auction.py
autonomous: true
requirements:
  - REQ-035
must_haves:
  truths:
    - "Core stage logic is covered by deterministic unit tests."
  artifacts:
    - path: "backend/tests/test_forward_market_planner.py"
      provides: "Stage 1 correctness checks"
    - path: "backend/tests/test_intelligence_orchestrator.py"
      provides: "Stage 2 delta behavior checks"
    - path: "backend/tests/test_dr_bounty_auction.py"
      provides: "Economic auction correctness checks"
  key_links:
    - from: "tests"
      to: "core agents"
      via: "public APIs only"
---

<objective>
Establish deterministic unit coverage for Stage 1/2 and DR auction mechanics.

Purpose: protect core economic and trigger logic from regressions.
Output: reliable pytest suite for foundational agents.
</objective>

<context>
@backend/src/agents/intelligence_agent/forward_market_planner.py
@backend/src/agents/intelligence_agent/orchestrator.py
@backend/src/agents/routing_agent/dr_bounty_auction.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add planner and delta trigger unit tests</name>
  <files>backend/tests/test_forward_market_planner.py, backend/tests/test_intelligence_orchestrator.py</files>
  <behavior>
    - Baseline output has 30-day entries and wake/sleep flags.
    - Delta export appears only on anomaly days.
  </behavior>
  <action>Write failing tests first for baseline construction and conditional Delta export, then implement/fix as needed until green.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_forward_market_planner.py backend/tests/test_intelligence_orchestrator.py -q</automated>
  </verify>
  <done>Planner and trigger behavior is validated under deterministic fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add DR auction clearing tests</name>
  <files>backend/tests/test_dr_bounty_auction.py</files>
  <behavior>
    - Cheapest bids clear first.
    - Deficit coverage and clearing price are computed correctly.
  </behavior>
  <action>Create bid fixtures and assert sorting, acceptance boundaries, and final settlement metrics.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_dr_bounty_auction.py -q</automated>
  </verify>
  <done>Auction logic is test-locked for cost-efficient dispatch decisions.</done>
</task>
</tasks>

<verification>
No flaky/randomized assertions; all tests run without network dependencies.
</verification>

<success_criteria>
Core agent logic has repeatable unit-level protection.
</success_criteria>

<output>
After completion, create `.planning/phases/12-unit-tests/12-01-SUMMARY.md`
</output>

