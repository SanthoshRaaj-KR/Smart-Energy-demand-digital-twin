---
phase: 13-integration-tests
plan: 01
type: execute
wave: 1
depends_on:
  - 12-02
files_modified:
  - backend/tests/integration/test_scenario_harness.py
  - backend/tests/integration/fixtures.py
autonomous: true
requirements:
  - REQ-037
must_haves:
  truths:
    - "E2E scenarios can be run repeatably without manual setup."
  artifacts:
    - path: "backend/tests/integration/test_scenario_harness.py"
      provides: "Scenario runner and common assertions"
  key_links:
    - from: "fixtures.py"
      to: "scenario harness"
      via: "shared deterministic setup"
---

<objective>
Build deterministic integration harness for end-to-end pipeline scenarios.

Purpose: validate cross-agent wiring, not just isolated functions.
Output: reusable scenario framework with shared fixtures.
</objective>

<context>
@backend/run_simulation.py
@backend/main.py
@backend/routes.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build deterministic scenario harness</name>
  <files>backend/tests/integration/test_scenario_harness.py, backend/tests/integration/fixtures.py</files>
  <action>Create fixture helpers for normal/minor/major/cascade/learning runs with deterministic seeds and artifact path helpers.</action>
  <verify>
    <automated>python -m pytest backend/tests/integration/test_scenario_harness.py -q</automated>
  </verify>
  <done>Scenario harness can execute all planned scenario types reproducibly.</done>
</task>
</tasks>

<verification>
Ensure scenarios do not require live external APIs.
</verification>

<success_criteria>
Integration suite has a stable foundation for full scenario assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/13-integration-tests/13-01-SUMMARY.md`
</output>

