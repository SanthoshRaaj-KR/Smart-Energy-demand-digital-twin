---
phase: 09-xai-audit-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/agents/routing_agent/unified_routing_orchestrator.py
  - backend/src/agents/routing_agent/phase8_xai_agent.py
  - backend/src/agents/xai/audit_ledger.py
autonomous: true
requirements:
  - REQ-026
  - REQ-027
must_haves:
  truths:
    - "Each simulation day emits a structured 7-phase trace."
    - "Every phase records decision, justification, math, and outcome."
  artifacts:
    - path: "backend/src/agents/xai/audit_ledger.py"
      provides: "Ledger contract and event builder"
    - path: "backend/src/agents/routing_agent/unified_routing_orchestrator.py"
      provides: "Stage event emission hooks"
  key_links:
    - from: "unified_routing_orchestrator.py"
      to: "audit_ledger.py"
      via: "phase event append"
---

<objective>
Create a strict, human-readable XAI ledger contract so Stage 1-4 decisions become regulator-auditable events.

Purpose: convert low-level logs into explainable phase evidence.
Output: typed 7-phase event stream collected during simulation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/src/agents/routing_agent/unified_routing_orchestrator.py
@backend/src/agents/routing_agent/phase8_xai_agent.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define 7-phase ledger contract</name>
  <files>backend/src/agents/xai/audit_ledger.py</files>
  <behavior>
    - Creating an event for any phase yields required fields.
    - Missing phase metadata raises explicit validation errors.
  </behavior>
  <action>Create dataclasses/types for phase events and daily ledger envelope. Include validation for phase index, mandatory reasoning fields, and serialization helper.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_xai_ledger_contract.py -q</automated>
  </verify>
  <done>Ledger contract exists and validates all 7 phase entries.</done>
</task>

<task type="auto">
  <name>Task 2: Emit events from waterfall and memory flows</name>
  <files>backend/src/agents/routing_agent/unified_routing_orchestrator.py, backend/src/agents/routing_agent/phase8_xai_agent.py</files>
  <action>Add event emission at each stage boundary (assessment, temporal, economic, spatial, fallback, settlement, learning) and attach computed rationale/math snippets.</action>
  <verify>
    <automated>python -m pytest backend/test_unified_routing_orchestrator.py -q</automated>
  </verify>
  <done>Running one simulation day produces in-memory 7-phase event payload without missing fields.</done>
</task>
</tasks>

<verification>
Ensure event payload is deterministic and phase order is always 1..7.
</verification>

<success_criteria>
All anomaly days produce complete phase-level reasoning records suitable for downstream export.
</success_criteria>

<output>
After completion, create `.planning/phases/09-xai-audit-ledger/09-01-SUMMARY.md`
</output>

